@model Projet2.ViewModels.InscriptionViewModel
<meta name="viewport" content="width=device-width, initial-scale=1.0">

@{
    Layout ="_LayoutGeneral";
}
<link rel=" stylesheet" href="~/css/Profile.css" />
<head>
<title>
@*   Profile @User.Identity*@
</title>
  
</head>


    <body class="body">

    @Html.HiddenFor(m=>m.Account.Id)
    @Html.HiddenFor(m=>m.Account.ProfileId)
    @Html.HiddenFor(m=>m.Account.ContactId)
    @Html.HiddenFor(m=>m.Account.InfoPersoId)
    @Html.HiddenFor(n=>n.Inventory.Id)
    @Html.HiddenFor(m=>m.Account.role)
               
<div class=" profileMenu">

        <ul class="nav flex-column">
             <li class="nav-item ">
                <a class="nav-link active" href="#">Paramètres du compte</a>
            <li class="nav-item ">
                    @Html.ActionLink("Ajouter du materiel", "CreateStuff", "Stuff", new {accountId = Model.Account.Id})
            </li>
            <li class="nav-item ">
                    @Html.ActionLink("Consulter le planning", "PlanningView", "Planning", new {accountId = Model.Account.Id})
            </li>
            
            <li class="nav-item ">
                @Html.ActionLink("Messagerie", "MessageBoardView", "Messagerie", new {accountId = Model.Account.Id})
            </li>
            
        </ul>
    </div>
    

    <h1> Bienvenue sur votre profil @Model.Account.Username </h1>
    <div class="container-fluid">
        <div class="row">
            <div class="column1">
                <section class="SectionDescription">

@*@using (Html.BeginForm("EditProfilePic", "Inscription", FormMethod.Post, new { enctype = "multipart/form-data" }))
{

                       
                        @Html.HiddenFor(m=> m.Profile.ImagePath)

                    <div class="form-floating mb-3">
                        <div>
                                @Html.LabelFor(model => model.Profile.ProfilImage, new { id = "imageLabel", @style = "text-align:center;" })
                                @Html.TextBoxFor(model => model.Profile.ProfilImage, new { placeholder = "Description", @type = "file" })
                        </div>
                    </div>
                    <div class="text-center">
                           <input type="file" name="ProfilImage" id="fileUpload" accept=".png,.jpg,.jpeg,.gif,.tif"/>
                           <img src="@Model.Profile.ImagePath" height="400" width="400" />
                        </div>

                        <input type="submit" value="Choisir cette image" class="BoutonValidationFormulaire" />

}
*@
                    <img src="@Model.Profile.ImagePath" height="400" width="400" class=" profimage" />
                    <div class="Text">
                        
                        
                        <h2>Details personnnels</h2>
                        <h3>Nom: @Model.Infos.LastName</h3> <h3 class="spec"> </h3>
                        <h3> Prenom: @Model.Infos.FirstName </h3> <h3 class="spec"> </h3>
                        
                        <h3>Date de naissance:@Model.Infos.Birthday </h3>
                        <h3>Adresse mail : @Model.Contact.EmailAdress</h3>
                    </div>
                </section>
            </div>
            <div class="column2">
                <section class="SectionContenu">
                    <fieldset id="FS1">
                        <legend class="leg"> Bio</legend>
                        <p> @Model.Profile.Bio</p>
                    </fieldset>
                    <fieldset id="FS2">
                        <legend class="leg"> Mes Jeux preferés: </legend>
                        <p>@Model.Profile.Games </p>
                    </fieldset>
                    <fieldset id="FS3">
                        <legend class="leg"> CompÃ©tences techniques</legend>
                        <p>
                            Light
                            GrÃ¢ce au Death Note, il est capable de tuer n'importe qui (exceptÃ© les shinigami), pour peu qu'il connaisse le nom et le visage de sa victime.
                        </p>
                    </fieldset>
                    <fieldset id="FS4">
                        <legend class="leg"> Autres compÃ©tences </legend>
                        <p>Light parvient Ã ler Rem, le Shinigami liÃ© au Death Note de Misa. Il atteindra'elle Ã©crive le nom de L dans son carnet.</p>
                    </fieldset>

                </section>
            </div>
        </div>
    </div>
    <div>
    <fieldset>
      <legend> Mon inventaire : (Stuff) </legend>
            <table border="1">
                <tr>
                    <th><h2>Nom</h2></th>
                    <th><h2>Description</h2></th>
                    <th><h2>Type</h2></th>
                    <th><h2>Etat</h2></th>
                    <th><h2>Reservation</h2></th>
                    <th><h2>Modif Stuff</h2></th>
                    <th><h2>Consulter</h2></th>
                    
                </tr>
                @foreach (var Stuff in Model.Stuffs)
                { 
                        <tr><th>Mon Stuff à moi</th></tr>
                        <tr>
                      <td><img src="@Stuff.ImagePath" height="200" width="200"  /></td>
                            <td>@Html.DisplayFor(mStuff => Stuff.Name)</td>
                            <td>@Html.DisplayFor(mStuffs => Stuff.Description)</td>
                            <td>@Html.DisplayFor(mStuffs => Stuff.Type)</td>
                            <td>@Html.DisplayFor(mStuffs => Stuff.State)</td>
                            <td>@Html.DisplayFor(mStuffs => Stuff.Reservation)</td>
                            <td>@Html.ActionLink("Modification", "EditStuff","Stuff", new { id = Stuff.Id })</td>
                            <td>@Html.ActionLink("Réservation ajouter le if", "AcceptationBookStuff","Stuff", new { id = Stuff.Id })</td>
                        </tr>
               
                 }
            }
        }
            </table>
    </fieldset>
    </div> 
    <div>
        <fieldset>
            <legend> Les denières publications: (Stuff) </legend>

            <table border="1">
                <tr>
                    <th><h2>Nom</h2></th>
                    <th><h2>Description</h2></th>
                    <th><h2>Auteur</h2></th>

                </tr>

                @foreach (var Publication in Model.Publications)
                {
                    <tr>
                        <td>@Html.DisplayFor(mPublication => Publication.Name)</td>
                        <td>@Html.DisplayFor(mPublication => Publication.Content)</td>
                        <td>@Html.DisplayFor(mPublication => Publication.Account.Username)</td>
                    </tr>
                }


            </table>
        </fieldset>

    </div>
    @Html.ActionLink("Voulez-vous vous déconnecter ?", "Deconnexion")
</body>